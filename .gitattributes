# Windows batch files must keep CRLF: cmd.exe can mis-parse labels in a
# file with bare LF endings, which breaks every goto in it.
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell scripts must keep LF even when checked out on Windows, or bash
# fails on the carriage returns.
*.sh text eol=lf
